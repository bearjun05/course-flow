# 미결정 정책 목록

> 백엔드 설계 및 운영 규칙에 영향을 주는 **답변이 필요한 질문들**을 한 곳에 모아둔 파일입니다.
> 상세 맥락은 `정책-초안.md`의 해당 섹션을 참고하세요.
> 답변은 `→ 답변:` 뒤에 적어주시면 정식 정책으로 정리합니다.

---

## 🔴 블로커 (백엔드 설계에 즉시 영향)

### 상태 전이

**Q-01. 상태를 뒤로 되돌릴 수 있나요?** (예: 촬영 → 교안)
- 맥락: `정책-초안.md` Section 1 자동/수동 전이 표에 "되돌리기" 정책 일부만 언급됨
- 영향: 백엔드의 상태 전이 검증 로직, 이력 관리
→ 답변:

**Q-02. "중단"에서 재개하면 어떤 상태로 복귀하나요?**
- 옵션: (a) 중단 직전 상태 자동 복구 (b) PM이 직접 재선택
- 영향: 프로젝트에 "이전 상태" 저장 필드 필요 여부
→ 답변:

**Q-03. 새 프로젝트 생성 시 초기 상태는 무조건 "기획"인가요?**
- 현재 프론트: 제작요청 폼 제출 시 "기획"으로 고정
- 영향: 생성 API의 기본값
→ 답변:

**Q-04. 태스크 순서가 강제인가요?**
- 예: 교안제작이 "대기"인데 촬영을 "진행"으로 바꿀 수 있나요?
- 현재 프론트: 순서 강제 없음
- 영향: 상태 전이 API의 순서 검증 로직
→ 답변:

**Q-05. 프로젝트 "완료" 조건: D-Day < 0 필수인가요?**
- "모든 장의 모든 강이 승인 완료되면 자동으로 완료"라고 정책에 있으나,
  "D-Day < 0 이어야 한다"와 함께 언급된 적 있음
- 옵션: (a) D-Day = 0 당일에도 완료 가능 (b) 반드시 D-Day < 0
- 영향: 자동 완료 이벤트 트리거 조건
→ 답변:

### 외부 연동 (Google Drive)

**Q-06. 강별 파일명 포맷**
- 한 장 안에 강이 여러 개(1-1, 1-2...) → 원본영상 폴더에도 파일이 여러 개 들어감
- 파일명에 "1-1", "1-2" 같은 라벨을 붙여서 구분?
- 포맷 규칙 예시: `{강의명}_{강라벨}_원본.mp4`? `{강라벨}.mp4`?
- 영향: Drive 파일 감지 → 강 매핑 로직
→ 답변:

**Q-07. Drive 루트 경로**
- 환경변수로 루트 디렉토리 지정한다고 했지만, 어떤 구조의 Shared Drive인지?
- 서비스 계정 권한 부여 방식은?
→ 답변:

---

## 🟡 MVP 전 결정 권장

### 에듀웍스 (외부 관계자)

**Q-08. 일정 탭 드래그 차단 외 추가 상호작용 허용 여부**
- 현재 모두 읽기 전용으로 확정됨
- 편집자가 편집 완료 시 UI에서 마킹할 수 있어야 하는지 재확인 필요 (검수자는 검수 토글 가능)
→ 답변:

**Q-09. 자막자의 요약노트(JSON) 업로드**
- 정책: Drive에 직접 올리면 자동 감지
- 현재 자동 감지가 어떤 식으로 동작할지 명확화 필요 (파일 확장자로 구분? 폴더 위치로 구분?)
→ 답변:

### 담당자 관리

**Q-10. 담당자 복수 배정 UI/데이터 모델**
- 정책: 역할별 2명 이상 중복 선택 가능 (슬랙 채널 멤버 드롭다운)
- 현재 프론트: 각 역할당 1명만 (`tutor?: string` 등)
- 영향: Project 타입 필드를 배열로 변경 + UI 개편
→ 답변:

**Q-11. PM 필드**
- 현재 프론트: "박진영"으로 고정 표시
- Project 타입에 `pm` 필드 없음
- 영향: DB 스키마, API 응답 모델
→ 답변:

### 검수 이력

**Q-12. "보완" 판정 후 재검수 시 이전 검수/피드백 이력**
- 옵션: (a) 덮어쓰기 (b) 이력 축적 (round/version으로 구분)
- 영향: 검수 데이터 스키마
→ 답변:

---

## 🟢 배포 후 결정해도 되는 항목

**Q-13. 슬랙 채널 검색 방식**
- 정책: 워크스페이스 채널 검색 + 드롭다운
- 현재 프론트: URL + ID 수동 입력
- Slack API 구체 활용 방안 (Conversations API)
→ 답변:

**Q-14. 슬랙 자동 알림 범위**
- 모든 상태 전이마다? 특정 이벤트만? (예: 자동 전이 시 다음 담당자에게만)
- 알림 내용 템플릿
→ 답변:

**Q-15. 되돌리기 연쇄 해제 범위**
- 정책: 되돌리면 해당 단계 이후 모든 단계가 연쇄 해제
- 구체 예: "편집·자막"을 되돌리면 검수/승인 모두 초기화 + 업로드된 파일은 유지? 파일도 삭제?
→ 답변:

**Q-16. 롤아웃 상태의 의미**
- 정책: "모든 장·강 승인 완료 시 자동 완료"로 결정됨
- 그렇다면 "롤아웃" 상태는 언제 사용?
- 옵션: (a) 과도기 상태로 계속 유지 (b) enum에서 제거
→ 답변:

**Q-17. Drive 폴더 강의명 변경 동기화**
- 강의명이 중간에 바뀌면 Drive 폴더 이름도 바뀌어야 하나요?
- 백오피스와 강의명이 동기화되어야 한다고 했는데, 그게 자동 처리되는지?
→ 답변:

**Q-18. 프로젝트 "삭제" 시 Drive/Slack 정리**
- 정책: 삭제 = 완전 제거, 복구 불가
- Drive 폴더도 함께 삭제? 유지?
- Slack 채널은 어떻게?
→ 답변:

---

## 📦 관련 파일

- 정책 본문: `/docs/policies/정책-초안.md`
- 대시보드 관련: Section 4
- 프로젝트 상세: Section 7
- 제작 요청 폼: Section 8
- 에듀웍스: Section 9
- 태스크 캘린더: Section 10
