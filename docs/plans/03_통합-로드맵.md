# 3️⃣ 통합 로드맵

> 플랜 1~2 이후 백엔드 개발자에게 인수인계하기까지의 전체 그림.

---

## 마일스톤

### M1. 코드 퀄리티 정리 ✅ 거의 완료
- 플랜 1번 (`01_코드-퀄리티-정리.md`) 참고
- H-1 ~ M-4 모두 완료, 빌드 통과
- 남은 항목: monday-board 968줄 분할 (난이도 높아 보류)

### M2. API 스키마 문서 ✅ 완료
- 플랜 2번 (`02_API-스키마-정의.md`) 참고
- 산출물: `docs/policies/API-스키마.md`
- 엔티티 필드 + 27개 엔드포인트 Request/Response 명세 + 권한

### M3. 문서 완결성 체크 ✅ 완료
- 정책 디렉토리 인덱스(`docs/policies/README.md`) 최신화
- 최상위 `docs/README.md`에 "백엔드 개발 착수 시 먼저 읽을 문서" 섹션 추가
- 구정책 파일(`영상-피드백-상세-설계.md`, `상태값목록.md`) 제거

### M4. 백엔드 인수인계 — 준비 완료
- 새 개발자가 아래 5개 문서만으로 시작 가능:
  1. `정책-초안.md` (비즈니스 룰)
  2. `API-스키마.md` (데이터 계약)
  3. `백엔드-할일.md` (구현 체크리스트)
  4. `미결정-정책.md` (남은 질문)
  5. 프론트 코드 (참고용, mock이 곧 예시 데이터)

---

## 문서 완결성 체크리스트

- [x] 엔티티 필드가 타입/필수/기본값까지 명시 (`API-스키마.md` §1)
- [x] 담당자 복수 배정: **배열**로 통일 (API-스키마 + 백엔드-할일에 명시, 프론트 코드 쪽은 마이그레이션 작업 남음)
- [x] API Request/Response 스키마 (`API-스키마.md` §2)
- [x] 상태 전이 다이어그램 및 트리거 (`정책-초안.md` §1 + `API-스키마.md` §3)
- [x] D-Day 계산식 + 경계값 (`정책-초안.md` §6)
- [x] 신호등: 수동 조작만, 자동 로직 없음 (`정책-초안.md` §6 — 자동 변경 로직 코드에서도 삭제됨)
- [x] 권한은 API 엔드포인트마다 명시 (`API-스키마.md` §2)
- [x] Drive 폴더 규칙 (`정책-초안.md` §2) + 파일명 정규식은 백엔드 결정 사항 (`백엔드-할일.md`)
- [x] 슬랙 템플릿 + 복수 담당자 멘션 규칙 (`정책-초안.md` §11)
- [x] 중단/재개 동작 (`정책-초안.md` §1 + `API-스키마.md` §2.2)
- [x] 리뉴얼 시 기존 폴더 재활용 규칙 (`정책-초안.md` §2)
- [x] 제작 요청 폼 필드 목록 (`제작요청-폼.md` + `API-스키마.md` `POST /api/projects`)

---

## 일정 제안 (러프)

| 주 | 작업 |
|---|------|
| W1 | 플랜 1 High 항목 + M1 Medium 일부 |
| W2 | 플랜 1 Medium 나머지 + 플랜 2 엔티티 스키마 작성 시작 |
| W3 | 플랜 2 엔드포인트 명세 작성 |
| W4 | M3 문서 완결성 체크 + M4 인수인계 준비 |

실제 일정은 PM 판단에 맡김. 위는 목표.

---

## 주의

- 코드 수정과 문서 수정이 **항상 같이** 가야 함. 코드만 바뀌거나 문서만 바뀌면 불일치 발생.
- 백엔드 개발자 투입 전에 **프론트를 더 늘리지 않기**. 지금도 600줄+ 컴포넌트가 있어 정리가 먼저.
- 담당자 배열 마이그레이션은 API 스키마 확정 후 한 번에 진행.
